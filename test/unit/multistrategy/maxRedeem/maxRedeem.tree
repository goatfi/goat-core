maxRedeem.t.sol
├── when the address is the zero address
│   └── it should return 0
└── when the address is not the zero address
    ├── when the address holds no vault shares
    │   └── it should return 0
    └── when the address holds vault shares
        ├── when the available liquidity converted to shares is greater than or equal to the balance of shares
        │   └── it should return the balance of shares
        └── when the available liquidity converted to shares is less than the balance of shares
            └── it should return the available liquidity converted to shares