setProtocolFeeRecipient.t.sol
├── when the caller is not the owner
│   └── it should revert
└── when the caller is the owner
    ├── when the protocol fee recipient is the zero address
    │   └── it should revert with ZeroAddress error
    └── when the protocol fee recipient is not the zero address
        └── it should set the protocol fee recipient and emit the ProtocolFeeRecipientSet event